# update

> Update the Linear CLI

## Usage

```
Usage:   linear update

Description:

  Update the Linear CLI

Options:

  -h, --help           - Show this help.
  --workspace  <slug>  - Target workspace (uses credentials)
  --bump               - Allow mise to update a pinned version selector
```
