# version

> Show build identity and protocol capabilities

## Usage

```
Usage:   linear version

Description:

  Show build identity and protocol capabilities

Options:

  -h, --help           - Show this help.
  --workspace  <slug>  - Target workspace (uses credentials)
  --json               - Output machine-readable build identity
```
